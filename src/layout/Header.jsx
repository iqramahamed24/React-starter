/**
 * 
 * components
 * 
 * 1.componenets must always start with a capital letter
 * 2.Must always return a single parent element
 * 3.Must be functions(normal functions or arrow functions)
 * 
 */

 const Header = () => {
  return (
    <div>
        <header>Header</header>
        <h1>React Starter</h1>
        </div>
  )
}
export default Header;